<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Portal | Edit</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1 class="animated-title">Student Portal </h1>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/courses">Courses </a></li>
                <li><a href="/register-courses">Course Registration </a></li>
                <li><a href="/hostels">Hostels </a></li>
                <li><a href="/register-hostels">Hostel Registration </a></li>
            </ul>
        </nav>
    </header>
    <main>
        <section id="home">
            <h2>Update Courses</h2>
            
        </section>
        <section id="studentForm">
           
            <form id="addCourseForm" action="/<%= student.id %>?_method=PUT" method="POST">
                <input type="hidden" name="_method" value="PUT">
                <label for="firstName">First name:</label>
                <input type="text" id="firstName" name="firstName" value="<%= student.firstName %>" required minlength="2">

                <label for="lastName">Last name:</label>
                <input type="text" id="lastName" name="lastName" value="<%= student.lastName %>" required minlength="2">

                <label for="studentId">Student ID:</label>
                <input type="text" id="studentId" name="studentId" value="<%= student.studentId %>" required pattern="[A-Z]{2}[0-9]{4}">

                <label for="department">Department:</label>
                <select id="department" name="department" required>
                    <option value="">Select your Department</option>
                    <option value="Software Engineering" <%= student.department === 'Software Engineering' ? 'selected' : '' %>>Software Engineering</option>
                    <option value="Cyber Security" <%= student.department === 'Cyber Security' ? 'selected' : '' %>>Cyber Security</option>
                </select>

                <label for="session">Session:</label>
                <select id="session" name="studentSession" required>
                    <option value="">Select your seesion</option>
                    <option value="2024/2025" <%= student.session === '2024/2025' ? 'selected' : '' %>>2024/2025</option>
                    <option value="2025/2026" <%= student.session === '2024/2025' ? 'selected' : '' %>>2025/2026</option>
                </select>
                
                <div class="courses-container">
                    <h2>Select Your Courses</h2>
                    <% courses.forEach(course => { %>
                        <div class="course">
                            <input type="checkbox" 
                                id="<%= course._id %>" 
                                name="courses" 
                                value="<%= course._id %>"
                                <%= student.courses.some(c => c.equals(course._id)) ? 'checked' : '' %>>
                            <label for="<%= course._id %>"><%= course.code %> (<%= course.name %>)</label>
                        </div>
                    <% }); %>
                </div>
                

                <button type="submit" class="register">Update</button>
            </form>
        </section>
        
    </main>
    <footer>
        <p>&copy; 2024 Student Portal Application</p>
    </footer>
</body>
</html>